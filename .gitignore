# User & hunt data — managed at runtime, not versioned
/data/users.json
/data/hunts/*.json

# OS / editor files
.DS_Store
**/.DS_Store
Thumbs.db
*.swp
*.swo

# git pull file
update.php

# config
push-config.js

# custom push provider (user-supplied, never committed)
/custom/