## AI agent guidance

- Whenever you change anything, bump `APP_VERSION` in `js/app.js` by at least `0.0.1` and keep any user-facing version display in sync.
- If the change is bigger, bump version by `0.1.0`
